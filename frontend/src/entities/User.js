export default class User {
    constructor(id, name, pictureUrl, activeStatus) {
        this.id = id;
        this.name = name;
        this.pictureUrl = pictureUrl;
        this.activeStatus = activeStatus;
    }
}